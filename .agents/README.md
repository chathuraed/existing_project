# Antigravity Legacy → Agents Migration Skill

This project contains a reusable Antigravity skill that helps migrate
legacy codebases into the `.agents/skills` architecture.

## Usage
Place the `.agent/skills` folder inside your repository root
and trigger the skill inside Antigravity:

@legacy-to-agents-migration
